E*TRADE runs from this Finance folder (headless worker + agents).

Location: %USERPROFILE%\Finance  (or this git clone)
Remote:   https://github.com/shaggychunxx-ui/Finance

Dual-PC option B (active — see DUAL_PC_DEPLOYMENT.md / ROLE_FLIP_B.md):
  BOXONE     role=broker    → OAuth + headless worker + orders (practice until dry_run off)
  AI-CODING  role=pipeline  → agents/backtests; pull quotes from share; no orders
  Share: C:\Users\Public\HelperDrop\FinanceShare
         (UNC from BOXONE: \\10.10.10.1\HelperDrop\FinanceShare)

Desktop ETrade Trader / Unified Trader GUIs: REMOVED (see ETRADE_HEADLESS.md).
Use Finance Agents.bat for research UI; background worker for trading.

How to update:
  1. Open this folder in GitHub Desktop (or: git pull)
  2. Restart background worker if needed:
     double-click "Start ETrade Background Service.vbs"
     or reboot (Startup + Run key auto-start)

Secrets stay local (gitignored):
  etrade_config.json, etrade_tokens.json, short_etrade_config.json,
  config.json, ui_prefs.json, output/

Old separate install (optional delete after testing):
  %LOCALAPPDATA%\Programs\ETrade Unified Trader
