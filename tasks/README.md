# Finance tasks

| Folder | Use |
|--------|-----|
| `pending/` | Open work (`target:` who runs it) |
| `done/` | Finished (add **Result**) |
| `failed/` | Hard stop / handoff cap |

Phone usually writes **Next** in `STATUS.md` via GitStatus; optional task files for multi-step work.
